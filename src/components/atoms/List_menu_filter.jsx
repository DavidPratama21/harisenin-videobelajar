const List_menu_filter = ({children}) => {
  return (
    <div className="py-2.5 px-3 bg-white text-dark-secondary hover:bg-other-base-background hover:text-dark-primary">
        <p className="font-medium text-sm leading-[140%] tracking-[0.2px]">{children}</p>
    </div>
  )
}

export default List_menu_filter